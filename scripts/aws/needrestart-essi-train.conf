# Never let needrestart (run by unattended-upgrades) restart the ESSI-FR training
# service: a restart kills a multi-day training run.
#
# Install: sudo cp ~/FRS/scripts/aws/needrestart-essi-train.conf /etc/needrestart/conf.d/essi-train.conf
$nrconf{override_rc}{qr(^essi-train\.service$)} = 0;
